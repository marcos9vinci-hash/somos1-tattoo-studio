import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.indicaai.app',
  appName: 'IndicaAi',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
